export enum RequestMethod {
    GET = 'get',
    POST = 'post',
    PUT = 'put',
    DELETE = 'delete',
    PATCH = 'patch',
    OPTIONS = 'options',
}

export enum ControllerParamType {
    Integer = 'interger',
    String = 'string',
    Object = 'object',
    Array = 'array',
    Binary = 'binary',
}
